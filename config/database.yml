default: &default
 adapter: postgresql
 encoding: unicode
 host: db
 username: postgres
 password:
 pool: 5

development:
 <<: *default
 database: taskmanager_development

test:
 <<: *default
 database: taskmanager_test